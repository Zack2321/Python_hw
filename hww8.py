# Input values for 'a' and 'b'
a = float(input("a : "))
b = float(input("b : "))

# Calculate the result
result = (a + b) * (a + b)

# Display the result
print(f"The result of ({a} + {b}) * ({a} + {b}) = {result}")
